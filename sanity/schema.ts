/*
  This is where you should be exporting your schema files from the 
  /schemaTypes folder
*/

import { type SchemaTypeDefinition } from 'sanity'


export const schema: { types: SchemaTypeDefinition[] } = {
  // Add schemas here
  types: [],
}
